
public enum MoveDirection
{
    Down,
    Up,
    Left,
    Right,
}

public enum Turn
{
    PlayerSet,
    PlayerMove,
    PlayerAttack,
    EnemyMove,
    EnemyAttack,
    Count,
}

public enum DiceType
{
    Normal,
    Wide,
    Powerful,
}

public enum Difficulty
{
    Easy,
    Normal,
    Hard,
}
