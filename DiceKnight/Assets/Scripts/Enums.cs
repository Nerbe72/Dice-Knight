
public enum MoveDirection
{
    Down,
    Up,
    Left,
    Right,
}

public enum Turn
{
    PlayerSet,
    PlayerMove,
    PlayerAttack,
    EnemyMove,
    EnemyAttack,
    Count,
}

public enum RollingTo
{
    Up,
    Down,
    Left,
    Right,
}

public enum DiceType
{
    Normal,
    Wide,
    Powerful,
}

public enum Difficulty
{
    Easy,
    Normal,
    Hard,
}

public enum Debuff
{
    Burn,
    Slow,
}

public enum Skill
{
    No,
    Fire,
    Ice,
}
