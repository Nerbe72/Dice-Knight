
public enum MoveDirection
{
    Up,
    Down,
    Left,
    Right,
    Stay,
}

public enum Turn
{
    PlayerSet,
    PlayerMove,
    PlayerAttack,
    EnemyMove,
    EnemyAttack,
    Count,
}

public enum DiceType
{
    Normal,
    Spear,
    Wide,
}

public enum Difficulty
{
    Easy,
    Normal,
    Hard,
}

public enum Debuff
{
    Burn,
    Slow,
}

public enum Skill
{
    No,
    Fire,
    Ice,
}

public enum Effect
{
    Rolling,
    AttackNormal,
    AttackSpear,
    AttackShotgun,
    BreakingTile,

}

public enum Background
{
    Title,
    Easy,
    Normal,
    Hard,
}
